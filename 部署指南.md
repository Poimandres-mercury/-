# 行星时 PWA · 完整部署指南

## 文件说明
```
planetary-hours-pwa/
├── index.html          ← 整个App（所有功能都在这一个文件里）
├── src/
│   └── engine.js       ← 行星时算法（NOAA精确算法）
├── public/
│   ├── manifest.json   ← PWA配置
│   ├── sw.js           ← 离线缓存
│   ├── icon-192.svg    ← App图标（双蛇杖）
│   └── icon-192.png    ← ⚠️ 需要你自己转换（见下方步骤）
└── vercel.json         ← 部署配置
```

---

## 第一步：准备图标

图标文件夹里有 `icon-192.svg`，你需要转成 PNG：

1. 用 Mac 打开 `icon-192.svg`（用 Safari 或 Chrome 打开）
2. 右键 → 存储为图像 → 保存为 `icon-192.png`
3. 复制一份改名为 `icon-512.png`

或者直接去这个网站转换：
**https://cloudconvert.com/svg-to-png**
- 上传 `icon-192.svg`
- 设置宽高 192×192，下载 → 命名 `icon-192.png`
- 再设置 512×512，下载 → 命名 `icon-512.png`
- 把两个文件放入 `public/` 文件夹

---

## 第二步：上传到 GitHub

1. 去 **https://github.com** 注册账号（免费）
2. 点右上角 **+** → **New repository**
3. 名称填：`planetary-hours`
4. 勾选 **Public** → 点 **Create repository**
5. 点 **uploading an existing file**
6. **把整个 `planetary-hours-pwa` 文件夹里所有文件拖进去**（注意保持文件夹结构）
7. 点 **Commit changes**

---

## 第三步：部署到 Vercel（免费全球访问）

1. 去 **https://vercel.com** 用 GitHub 账号登录
2. 点 **Add New → Project**
3. 找到刚才的 `planetary-hours` 仓库，点 **Import**
4. 什么都不用改，直接点 **Deploy**
5. 等待约1分钟，完成后会给你一个网址，类似：
   `https://planetary-hours-xxx.vercel.app`

✅ **这个网址全球任何人都能访问！**

---

## 第四步：安装到手机桌面

### iPhone / iPad：
1. 用 **Safari** 打开你的 Vercel 网址
2. 点底部分享按钮（方形+箭头图标）
3. 选 **「添加到主屏幕」**
4. 名称填「行星时」→ 点添加
5. 桌面出现图标，点开就像原生App一样！

### Android：
1. 用 **Chrome** 打开网址
2. 点右上角三个点菜单
3. 选 **「添加到主屏幕」** 或 **「安装应用」**
4. 确认安装即可

### 电脑（Mac/Windows）：
1. 用 Chrome 打开网址
2. 地址栏右边会出现安装图标（+号）
3. 点击安装，桌面出现应用图标

---

## 分享给朋友

直接把 Vercel 网址发给朋友，他们打开后同样可以安装到桌面，完全免费。

---

## App 功能说明

### 📍 当前行星时
- 自动定位，实时显示当前行星时
- 每秒检查，行星时切换时自动刷新
- 显示：行星符号、名称、时间段、进度条、剩余时间
- **适合活动**：详细建议
- **魔法仪式**：点「查看魔法仪式」展开

### 📋 全天行星时
- 按公历日期 00:00 开始完整排列
- 可选择任意日期查看
- 点击每条展开活动建议 + 魔法仪式
- 自动高亮当前行星时

### 🔍 历史查询
- 输入任意年月日
- 支持当前位置或手动输入城市名/经纬度
- 查询任意历史日期、任意地区的行星时

---

## 常见问题

**定位不准确？**
→ 允许浏览器定位权限，Safari/Chrome设置里找「位置」改为「允许」

**图标不显示？**
→ 确保 `public/icon-192.png` 和 `icon-512.png` 文件存在

**无法离线使用？**
→ 需要先联网打开一次，Service Worker会缓存页面，之后可离线使用
